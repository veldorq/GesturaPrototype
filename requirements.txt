# AccessAble - Gesture-Based Web Navigation
# Python 3.10+ required

# Core dependencies (rule-based system)
opencv-python>=4.8.0
mediapipe==0.10.9  # Version 0.10.9 has solutions API (required for code compatibility)
pyautogui>=0.9.54
numpy<2.0.0  # TensorFlow 2.15.0 requires numpy < 2.0

# CNN/ML dependencies (for CNN-based gesture recognition)
tensorflow==2.15.0  # Compatible with protobuf 3.20.3 and mediapipe 0.10.9
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
protobuf==3.20.3  # Required for MediaPipe 0.10.9 and TensorFlow 2.15.0 compatibility

# HIGH-PRIORITY: Stability & Smoothness Improvements
filterpy>=1.4.5          # Kalman filtering for confidence smoothing (mandatory)
scipy>=1.11.0            # Savitzky-Golay filtering for jitter reduction (mandatory)
# threading/concurrent.futures are Python stdlib (no install needed)

# OPTIONAL: Safe Performance Enhancements
imutils>=0.5.4           # Consistent frame preprocessing
pydantic>=2.5.0          # Config validation (prevents silent bugs)
rich>=13.7.0             # Enhanced debugging (disable-able in production)
